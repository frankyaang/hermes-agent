# 核查

- English name: `audit`
- 中文名：核查
- Type: `audit`
- Default pipeline: `audit_pipeline`
- Private memory: `true`

检查结论一致性、字段完整性、流程状态和审计日志

## Pipeline

默认执行入口记录在 `pipeline/audit_pipeline.json`。

## Memory

私域经验记录在 `skill_mem/`，用于沉淀该 Skill 自己的经验。
