# 过程汇报

- English name: `briefing`
- 中文名：过程汇报
- Type: `status_report`
- Default pipeline: `briefing_pipeline`
- Private memory: `true`

关键节点完成后生成自然语言过程汇报，服务所有主流程节点

## Pipeline

默认执行入口记录在 `pipeline/briefing_pipeline.json`。

## Memory

私域经验记录在 `skill_mem/`，用于沉淀该 Skill 自己的经验。
