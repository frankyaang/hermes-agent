# 看板渲染

- English name: `dashboard_html`
- 中文名：看板渲染
- Type: `ui_render`
- Default pipeline: `dashboard_html_pipeline`
- Private memory: `true`

消费运营看板结构化输出并渲染HTML/PDF，不改写业务内容

## Pipeline

默认执行入口记录在 `pipeline/dashboard_html_pipeline.json`。

## Memory

私域经验记录在 `skill_mem/`，用于沉淀该 Skill 自己的经验。
