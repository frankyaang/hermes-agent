# 渲染专家

- English name: `render_expert`
- 中文名：渲染专家
- Private memory: `true`

负责HTML/PDF呈现质量，确保渲染层不改写业务内容

## Callable Skills

- `dashboard_html`（看板渲染）
- `briefing`（过程汇报）

## Pipeline

默认专家编排入口记录在 `pipeline/default_pipeline.json`。

## Memory

私域经验记录在 `expert_mem/`，用于沉淀该专家的协同经验。
